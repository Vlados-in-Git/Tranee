<?xml version="1.0" encoding="utf-8" ?>
    <ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tranee.views.CurrentSchemaPage"
            >

    <VerticalStackLayout Margin="0,10,40,0" >

      
        <Button Text="AddTestSchema" Command="{Binding AddTestSchema}" />

        <Label Text="Some of text" FontSize="18" FontAttributes="Bold" />

        <CollectionView x:Name="TemplateList" ItemsSource =" {Binding Templates} " >
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="10">

                        <Label Text="{Binding Name}"/>
                        <Label Text="{Binding Description }" />

                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            
        </CollectionView>




    </VerticalStackLayout>
    
    
</ContentPage>